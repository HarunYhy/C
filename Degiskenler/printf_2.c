#include <stdio.h>   /* Bu satırda consola bişeyler yazdırmak için kütüphane ekledik */

void main(){
    
    // \n imleci alt satıra geçirir
    // \r imleci satır başına alır
    // \t imleci tab kadar sağa kaydırır
    // \b imleci bi sola kaydırır
    
    printf("Merhaba\nTimur\rnasılsın\biyi\tmisin?");
    /* Merhaba yazıp alt satıra geçer Timur yazar imleci satır başına alır nasılsını timurun üstüne yazar
    bi geri gider n'nin üzerine iyi yazar tab atar misin? yazar   */            
}